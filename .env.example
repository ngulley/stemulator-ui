# ──────────────────────────────────────────────────────────
# STEMulator Frontend — Environment Variables
# ──────────────────────────────────────────────────────────
# Copy this file to .env:
#   cp .env.example .env
#
# For LOCAL DEVELOPMENT with the backend running on :8080,
# leave VITE_API_URL empty — the Vite proxy handles it.
#
# For PRODUCTION or pointing to a remote backend, set the
# full URL to the API server.
# ──────────────────────────────────────────────────────────

# Backend API base URL
# Leave empty for local dev (Vite proxy forwards /stemulator/* to localhost:8080)
# Set for production: https://api.stemulator.edu/stemulator/v1
VITE_API_URL=
